export enum PaymentMethod {
  AYAPAY,
  KBZPAY,
  WAVEPAY,
}

export enum PaymentStatus {
  pending = "pending",
  completed = "completed",
  failed = "failed",
}

export enum PlanType {
  MONTHLY,
  ANNUALLY,
}
